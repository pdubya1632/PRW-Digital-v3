import { Link } from "@remix-run/react";

export const loader = async() => {
    return new Response
}

export default function ProjectsRoute() {
    return (
        <main>
            <h1>Projects</h1>
        </main>
    )
}